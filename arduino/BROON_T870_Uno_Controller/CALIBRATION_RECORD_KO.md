# BROON T870 Uno 교정 기록

이 기록은 출력 잠금 `BROON_ENABLE_ACTUATOR_OUTPUTS` 및 조향 교정 잠금
`BROON_STEERING_CALIBRATION_CONFIRMED`, E-stop 극성 확인 잠금
`BROON_ESTOP_POLARITY_CONFIRMED`를 **0에서 변경하기 전에**
완성·검토·서명해야 한다. 현재 세 잠금은 모두 `0`이며, 이 양식에 빈 값이
남아 있으면 어떤 잠금도 변경하지 않는다.

측정값을 추정하지 않는다. 모든 칸을 현재 T870에서 직접 측정하고 서명한다.

## 공통 안전 전제

- 24 V 물리 차단장치가 즉시 접근 가능한 상태에서 작업한다.
- Uno, 수신기, 모터 드라이버의 신호 GND를 공통 접지하고, 각 전원 경로의
  퓨즈를 확인한다.
- 엔코더 입력 전압이 Uno 입력 허용 범위인지 계측으로 확인한다. 확신이 없으면
  연결하지 않는다.
- 출력·방향·교정 시험은 바퀴를 지면에서 완전히 띄운 상태에서만 수행한다.
- CAN은 RC/ROS 기반 차량 제어가 검증될 때까지 의도적으로 보류한다. D10–D13/SPI는
  예약 상태로 유지하고, CAN은 이 안전 기준선에 섞지 않은 별도 도입·검증 단계에서
  다룬다.
- 후륜 속도 피드백은 없다. 속도/엔코더 피드백은 전륜 D2/D3 엔코더만 사용한다.

## 교정 및 서명

| 항목 | 측정값/확인값 | 단위·근거 |
|---|---|---|
| 전륜 구동 PWM / DIR 핀 | ____________________ | 핀 번호 |
| 후륜 구동 PWM / DIR 핀 | ____________________ | 핀 번호 |
| 조향 PWM / DIR 핀 | ____________________ | 핀 번호 |
| 전륜 구동 방향 반전 플래그 | ____________________ | true / false |
| 후륜 구동 방향 반전 플래그 | ____________________ | true / false |
| 조향 방향 반전 플래그 | ____________________ | true / false |
| 전륜 엔코더 A / B 핀 | ____________________ | 핀 번호 |
| 전륜 정방향 엔코더 부호 | ____________________ | + / - 및 실제 이동 방향 |
| 전륜 바퀴 정방향 10회전 누적 카운트 | ____________________ | count |
| 전륜 바퀴 역방향 10회전 누적 카운트 | ____________________ | count |
| 정·역방향 절대 카운트 비교 | ____________________ | `abs(정방향)/10` 와 `abs(역방향)/10` 및 판정 |
| 계산된 바퀴 1회전당 카운트 | ____________________ | count/rev |
| 타이어 둘레 | ____________________ | m |
| 조향 전기적 유효 ADC 범위 | ____________________ | ADC |
| 좌 기계 끝 / 좌 안전 끝 / 중앙 / 우 안전 끝 / 우 기계 끝 ADC | ____________________ | ADC, 좌→우 순서 |
| 조향 ADC가 우측으로 증가하는지 | ____________________ | true / false |
| RC 조향 최소 / 중앙 / 최대 펄스폭 | ____________________ | µs |
| RC 스로틀 최소 / 중앙 / 최대 펄스폭 | ____________________ | µs |
| RC AUX 최소 / 중앙 / 최대 펄스폭 | ____________________ | µs |
| 전륜 구동 최소 유효 PWM / 최대 안전 PWM | ____________________ | PWM |
| 후륜 구동 최소 유효 PWM / 최대 안전 PWM | ____________________ | PWM |
| 조향 최소 유효 PWM / 최대 안전 PWM | ____________________ | PWM |
| E-stop 핀 | ____________________ | 핀 번호 |
| E-stop 해제 시 A4 원시 레벨 | ____________________ | HIGH / LOW, 실측 |
| E-stop 활성 시 A4 원시 레벨 | ____________________ | HIGH / LOW, 실측 |
| E-stop 설정 극성 | ____________________ | active-high / active-low |
| E-stop 독립 검증 극성 | ____________________ | active-high / active-low, 설정값과 일치 판정 |
| ROS 명령 타임아웃 | ____________________ | ms |
| RC 타임아웃 | ____________________ | ms |
| 방향 전환 대기 시간 | ____________________ | ms |
| 조향 무진행(stall) 창 | ____________________ | ms |

## 전사 및 잠금 변경 점검

1. 측정·서명된 기록을 `ControllerConfig.h` 값으로 전사한다.
2. 정방향과 역방향의 10회전 카운트는 **부호가 반대**인지와
   `abs(count)/10`의 절대 크기가 일관적인지를 비교한다. 부호를 포함한 두 수를
   단순 평균하거나 역방향의 음수 부호 때문에 불일치로 숨기지 않는다.
3. RC 조향·스로틀·AUX의 최소/중앙/최대는 `ControllerConfig.h`의
   `kRcSteerCalibration`, `kRcThrottleCalibration`, `kRcAuxCalibration`에 각각
   따로 전사한다. `kRcTimeoutMs`의 현재 기본값은 100 ms이지만 현재 T870 시험 결과와
   승인 없이 바꾸지 않는다. AUX 범위 안에 ROS/RC 임계값이 올바른 순서로
   들어오는지도 독립 검토한다.
4. E-stop은 출력 잠금과 24 V를 끈 상태에서 해제·활성 A4 레벨을 모두 실측한다.
   첫 시험 결과를 `kEstopConfiguration.configuredPolarity`에, 독립 재검증 결과를
   `verifiedPolarity`에 전사한다. 두 값이 같은 유효 극성이고 서명이 끝난 뒤에만
   `BROON_ESTOP_POLARITY_CONFIRMED`를 1로 변경한다. 미확인 또는 불일치 상태에서는
   소프트웨어가 E-stop 활성으로 간주하고 ARM을 거부해야 한다.
5. E-stop, 출력 잠금, 조향 교정 잠금, 유효 구성, 중립 대기, 타임아웃을
   리프트 휠 상태에서 모두 재확인한다. 이 문서의 완성만으로 출력이 허가되지는
   않는다.
